mutation AddEntry($object: ApplicationInsertInput!) {
  insertOneApplication(data: $object) {
    _id
    anzsco_code
    submitted_on
    received_on
    days
    outcome
    stream
    location
    comment
    created_at
  }
}